Every file in this directory is used to configure environment variables for nest services declared in the docker-compose.yml.

**Naming convention:** <container_name>.env is used by the service <container_name>
    